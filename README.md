A simple, accessible task manager built with HTML, CSS and vanilla JavaScript.

Features:
- Add tasks with the input form.
- Mark tasks complete / active.
- Delete tasks.
- Filter tasks (All / Active / Completed).
- Clear all completed tasks.
- Tasks persist in `localStorage`.
